spring:
  datasource:
    url: jdbc:mysql://localhost:3306/novelpot
    username: novelpot
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql   # 추가 해준 부분
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        show_sql: true